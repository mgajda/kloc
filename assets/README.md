# assets

`gpt2.tiktoken` — the GPT-2 (r50k_base) BPE mergeable-ranks vocabulary,
originally distributed by OpenAI (MIT License) with the `tiktoken` project.
Used by the `--tokens` mode (feature `tokens`) via the gigatoken tokenizer.
